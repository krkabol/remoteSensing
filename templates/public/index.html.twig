{% extends 'base.html.twig' %}

{% block title %}Home - Remote Sensing{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row align-items-center">
        <div class="col-lg-6">
            <h1 class="display-4 fw-bold">Welcome to Remote Sensing</h1>
            <p class="lead text-muted">
                A powerful platform for remote sensing data storage and visualization.
            </p>
            <div class="d-grid gap-2 d-md-flex justify-content-md-start">
                <a href="{{ path('app_public_about') }}" class="btn btn-primary btn-lg">
                    <i class="bi bi-info-circle"></i> Learn More
                </a>
                {% if app.user %}
                <a href="{{ path('app_internal_index') }}" class="btn btn-success btn-lg">
                    <i class="bi bi-speedometer2"></i> Go to Internal Section
                </a>
                {% else %}
                <a href="{{ path('app_login') }}" class="btn btn-dark btn-lg">
                    <i class="bi bi-github"></i> Login with GitHub
                </a>
                {% endif %}
            </div>
        </div>
        <div class="col-lg-6 text-center">
              </div>
    </div>
</div>
{% endblock %}
